{
  "name": "root",
  "version": "1.0.0",
  "private": "true",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "website": "yarn workspace @chakra-ui/pro-website"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
